<header>
  <div class="container">
    <a href="<%= page_path(@conn, :index) %>">Diskusi</a>

    <nav>
      <a href="<%= auth_path(@conn, :register) %>">Register</a>
      <a href="<%= auth_path(@conn, :login) %>">Login</a>
    </nav>
  </div>
</header>